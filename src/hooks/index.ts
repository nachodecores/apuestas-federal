/**
 * CUSTOM HOOKS - EXPORTACIONES CENTRALIZADAS
 * 
 * Este archivo exporta todos los custom hooks para facilitar los imports.
 * 
 * USO:
 * import { useDashboardData, useUserStats, useTeamMapping } from '@/hooks';
 */

export * from './useDashboardData';
export * from './useUserStats';
export * from './useTeamMapping';

